# 04-tickets.md
# Epic: PTT-REPAIRS-07-NT8-BULK-SKIP
# Phase: 3 -- Ticket Generation
# Source plan: 02-architecture-plan.md (REVIEW_PASS, Revision 1)
# Generated by: ptt-architect

---

## BASELINE (full epic start)

```
Total: 501 | Passed: 19 | Failed: 450 | Skipped: 32
```

Engineer must verify this baseline before touching any code:

```powershell
dotnet test src/PropTraderTools/ --no-build 2>&1 | Select-String "Total|Passed|Failed|Skipped"
```

If the baseline does not match, STOP and report to Director before proceeding.

---

## Skip Attribute (canonical -- copy verbatim)

```csharp
[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]
```

- ASCII-only. No curly quotes. No Unicode. Inline string only. No SkipReason constants.

---

## File in scope

`src/PropTraderTools/CopyEngineTests.cs`

**No other file is touched. No production code is changed.**

---

---

# T1 -- OPTION A Bulk Skip + OPTION B Verified Skip (Ticket 1 of 2)

**Ticket ID:** T1  
**Title:** Apply NT8-runtime Skip to all-TypeInit classes (OPTION A) and 2 partially-TypeInit classes (OPTION B)

---

## Spec Requirement IDs Satisfied

- PLAN-SEC4: Class map -- CopyEngineTests, CopyEngineB75Tests, B77QxRaceGuardTests, B78TargetDispatchTests, BwaveCycTaR7HelperTests (OPTION A)
- PLAN-SEC5-OPTION-A: Bulk replace bare [Fact] within class range for all-TypeInit classes
- PLAN-SEC5-OPTION-B: B79BeAllTargetSnapshotTests (7 TypeInit, 1 passing), B79BeReplaceAttemptGuardTests (1 TypeInit, 2 passing)
- PLAN-SEC6: OPTION B pre-analysis for B79BeReplaceAttemptGuardTests
- PLAN-SEC6: OPTION B pre-analysis for B79BeAllTargetSnapshotTests
- PLAN-SEC7: Ticket 1 scope definition
- PLAN-SEC8: Baseline and SCAN-5 Ticket 1 target
- PLAN-SEC9: All constraints (no production code, ASCII-only, no double-skip, no skip-passing-tests)
- PLAN-SEC10: 7-scan checklist
- PLAN-SEC13: Hard-link sync after edit

---

## Scope: Test Classes in This Ticket

| Class | Strategy | Line Range | Active [Fact] | Skip Target | Already-Skipped to Preserve |
|---|---|---|---|---|---|
| CopyEngineTests | OPTION A | 16-4236 | 188 | 188 | 17 |
| CopyEngineB75Tests | OPTION A | 4237-4905 | 47 | 47 | 14 (7 single + 7 multi-line) |
| B77QxRaceGuardTests | OPTION A | 4906-5128 | 8 | 8 | 0 |
| B78TargetDispatchTests | OPTION A | 5307-5388 | 8 | 8 | 0 |
| BwaveCycTaR7HelperTests | OPTION A | 7272-EOF | 34 | 34 | 1 (line 8059) |
| B79BeAllTargetSnapshotTests | OPTION B | 5478-5615 | 8 | 7 | 0 |
| B79BeReplaceAttemptGuardTests | OPTION B | 5625-5683 | 3 | 1 | 0 |

**Classes NOT in scope for this ticket (do not touch):**
- B78QxFollowerStopTests (lines 5129-5306)
- B78CancelFollowerGuardTests (lines 5389-5477)
- B79BeRetryAtmTriggerTests (lines 5692-5765)
- B79BeReplaceFallbackTests (lines 5766-5822)
- B79CancelRaceGuardTests (lines 5823-6455)
- BwaveCycT1R1BeHelperTests (lines 6469-6678)
- BwaveCycTaR2HelperTests (lines 6686-6811)
- BwaveCycTaR3HelperTests (lines 6812-7109)
- BwaveCycTaR6HelperTests (lines 7110-7268)

---

## Pre-Conditions

Engineer MUST measure a fresh baseline at the start of this ticket:

```powershell
dotnet test src/PropTraderTools/ --no-build 2>&1 | Select-String "Total|Passed|Failed|Skipped"
```

Record the output. Expected: `Total: 501 | Passed: 19 | Failed: 450 | Skipped: 32`

If the numbers differ from this baseline, STOP and report before making any changes.

---

## Method-Level Changes

### OPTION A: CopyEngineTests (lines 16-4236)

**Action:** Within lines 16-4236, replace every bare `[Fact]` annotation (no existing Skip argument)
with `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]`.

**Mechanism:** Scoped regex replace: `^\s*\[Fact\]` -> `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]` applied only within lines 16-4236.

**Preservation constraint:** Do NOT alter any `[Fact(Skip...]` line (single-line or multi-line format).
There are 17 existing `[Fact(Skip...)]` annotations in this range. They must survive unchanged.

**Net change:** 188 bare `[Fact]` -> `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]`

---

### OPTION A: CopyEngineB75Tests (lines 4237-4905)

**Action:** Within lines 4237-4905, replace every bare `[Fact]` annotation with the Skip attribute.

**Preservation constraint:** Do NOT alter any `[Fact(Skip...]` line. There are 14 existing Skip annotations:

| Format | Lines |
|---|---|
| Single-line `[Fact(Skip = "...")]` | 4532, 4570, 4576, 4582, 4674, 4735, 4824 |
| Multi-line (`[Fact(` on one line, `  Skip = "..."` on next) | 4538, 4546, 4554, 4562, 4844, 4852, 4861 |

Multi-line format example to preserve (do NOT flatten or alter):
```csharp
[Fact(
  Skip = "...existing skip reason...")]
```

**Net change:** 47 bare `[Fact]` -> `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]`

---

### OPTION A: B77QxRaceGuardTests (lines 4906-5128)

**Action:** Within lines 4906-5128, replace all bare `[Fact]` with the Skip attribute.

**Preservation constraint:** 0 existing Skip annotations in this range. No preservation needed.

**Net change:** 8 bare `[Fact]` -> `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]`

---

### OPTION A: B78TargetDispatchTests (lines 5307-5388)

**Action:** Within lines 5307-5388, replace all bare `[Fact]` with the Skip attribute.

**Preservation constraint:** 0 existing Skip annotations in this range. No preservation needed.

**Net change:** 8 bare `[Fact]` -> `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]`

---

### OPTION A: BwaveCycTaR7HelperTests (lines 7272-EOF)

**Action:** Within lines 7272-EOF, replace all bare `[Fact]` with the Skip attribute.

**Preservation constraint:** 1 existing `[Fact(Skip...)]` at line 8059 already uses the exact
target string. Do NOT double-skip it.

**Net change:** 34 bare `[Fact]` -> `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]`

---

### OPTION B: B79BeAllTargetSnapshotTests (lines 5478-5615)

**MANDATORY first step:** Run the test filter and record which tests fail with TypeInitializationException:

```powershell
dotnet test src/PropTraderTools/ --no-build --filter "FullyQualifiedName~B79BeAllTargetSnapshotTests" 2>&1
```

**Expected outcome:** 7 tests fail with TypeInitializationException, 1 test passes.

The 8 test methods in this class (lines 5490-5613):

| Method Name | Line | Architect Inference |
|---|---|---|
| `T_B79_BE_01_TargetSnapshotStateOk_Working_Included` | 5491 | Likely TypeInit (OrderState.Working access triggers NinjaTrader.Cbi assembly load) |
| `T_B79_BE_02_TargetSnapshotStateOk_Accepted_Included` | 5507 | Likely TypeInit |
| `T_B79_BE_03_TargetSnapshotStateOk_Submitted_Included` | 5523 | Likely TypeInit |
| `T_B79_BE_04_TargetSnapshotStateOk_Initialized_Included` | 5540 | Likely TypeInit |
| `T_B79_BE_05_TargetSnapshotStateOk_TriggerPending_Included` | 5556 | Likely TypeInit |
| `T_B79_BE_06_TargetSnapshotStateOk_Filled_Excluded` | 5572 | Likely TypeInit |
| `T_B79_BE_07_TargetSnapshotStateOk_Cancelled_Excluded` | 5587 | Likely TypeInit |
| `T_B79_BE_08_TargetSnapshotStateOk_ExactlyFiveStates` | 5603 | Likely PASSES (only `accepted.Length`, no OrderState enum access triggering assembly load) |

**Apply Skip ONLY to the 7 tests confirmed TypeInit by the filter run.**  
Do NOT apply Skip to the 1 passing test, regardless of which test it is.

**Exact Skip line to insert before each TypeInit-failing [Fact]:**
```csharp
[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]
```

**Verification step:** After applying, re-run the filter. Confirm:
- 7 tests show as `Skipped`
- 1 test still shows as `Passed`
- No test changes from `Passed` to `Failed` or `Skipped` unexpectedly

---

### OPTION B: B79BeReplaceAttemptGuardTests (lines 5625-5683)

**MANDATORY first step:** Run the test filter and record which test fails with TypeInitializationException:

```powershell
dotnet test src/PropTraderTools/ --no-build --filter "FullyQualifiedName~B79BeReplaceAttemptGuardTests" 2>&1
```

**Expected outcome:** 1 test fails with TypeInitializationException, 2 tests pass.

The 3 test methods in this class:

| Method Name | Line | Architect Inference |
|---|---|---|
| `T_B79_RG_01_BeReplaceAttempts_FieldExists` | 5636 | Uncertain -- `typeof(CopyEngine).GetField(...)` behavior depends on CLR state |
| `T_B79_RG_02_BeReplaceAttempts_StartsEmpty` | 5652 | LIKELY TypeInit (calls `CopyEngine.Instance` directly at line 5654) |
| `T_B79_RG_03_BeReplaceAttempts_GateIsAtThree` | 5673 | LIKELY PASSES (pure int comparison, no NT8 type access) |

**Apply Skip ONLY to the 1 test confirmed TypeInit by the filter run.**  
Do NOT apply Skip to the 2 passing tests.

**Exact Skip line:**
```csharp
[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]
```

**Verification step:** After applying, re-run the filter. Confirm:
- 1 test shows as `Skipped`
- 2 tests still show as `Passed`

---

## Post-Conditions (after Ticket 1 complete)

Run the full test suite:

```powershell
dotnet test src/PropTraderTools/ --no-build 2>&1 | Select-String "Total|Passed|Failed|Skipped"
```

**Expected SCAN-5 targets:**

| Metric | Required |
|---|---|
| Total | 501 (unchanged -- no tests added or removed) |
| Passed | >= 19 (must NOT decrease from baseline) |
| Failed | <= 157 (baseline 450 - 293 new Skips) |
| Skipped | >= 325 (baseline 32 + 293 new Skips) |

Delta: +293 Skipped, -293 Failed.

---

## 7-Scan Checklist -- Ticket 1

Engineer must run and PASS all 7 scans before marking T1 complete.

```
SCAN-1: grep -c "lock(" src/PropTraderTools/CopyEngineTests.cs
        -> Required: 0
        -> Failure action: revert and report

SCAN-2: Select-String -Pattern "[^\x00-\x7F]" src/PropTraderTools/CopyEngineTests.cs
        (or equivalent non-ASCII check on all changed lines)
        -> Required: 0 non-ASCII characters in any [Fact(Skip = "...")] line added in this ticket
        -> Failure action: find and remove non-ASCII character, re-run

SCAN-3: dotnet build src/PropTraderTools/ --no-restore 2>&1 | Select-String "Error"
        -> Required: 0 CS error lines
        -> Failure action: fix compilation error before proceeding

SCAN-4: dotnet build src/PropTraderTools/ 2>&1 | Select-String " Error\(s\)"
        -> Required: "0 Error(s)" in build summary
        -> Failure action: fix compilation error before proceeding

SCAN-5: dotnet test src/PropTraderTools/ --no-build 2>&1 | Select-String "Total|Passed|Failed|Skipped"
        -> Required: Total=501, Passed>=19, Failed<=157, Skipped>=325
        -> Failure action: if Passed dropped -> revert; if Skipped count wrong -> recount applied Skips

SCAN-6: powershell -File .\deploy-sync.ps1
        -> Required: output contains "SYNC COMPLETE"
        -> Failure action: check deploy-sync.ps1 for errors, do NOT proceed without SYNC COMPLETE

SCAN-7: $f = Get-Item "src\PropTraderTools\CopyEngineTests.cs"; $f.LinkType + " count=" + $f.HardLinkCount
        -> Required: LinkType = "HardLink" AND HardLinkCount >= 2
        -> IMPORTANT: count=1 means NOT hard-linked (regular file -- SCAN-6 sync FAILED)
        -> Failure action: re-run deploy-sync.ps1, verify SYNC COMPLETE, then re-check SCAN-7
```

---

## Constraints Checklist -- Ticket 1

- [ ] ONLY `src/PropTraderTools/CopyEngineTests.cs` modified. No other file touched.
- [ ] Zero production code changes.
- [ ] Skip string is exactly: `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]`
- [ ] Skip string is ASCII-only (no Unicode, no curly quotes, no em-dashes).
- [ ] No `lock()` anywhere in the file.
- [ ] No `throw` statements added.
- [ ] No new `SkipReason` constants, variables, or fields added.
- [ ] Existing `[Fact(Skip...)]` annotations are NOT modified (17 in CopyEngineTests, 14 in CopyEngineB75Tests, 1 in BwaveCycTaR7HelperTests).
- [ ] OPTION B: filter run BEFORE applying any Skip for B79BeAllTargetSnapshotTests.
- [ ] OPTION B: filter run BEFORE applying any Skip for B79BeReplaceAttemptGuardTests.
- [ ] OPTION B: Skip applied ONLY to TypeInit-failing tests (not passing tests).

---

## Engineer Notes -- Ticket 1

- Record actual pre-change baseline from `dotnet test` run at the start of this ticket.
- For OPTION A: use scoped replace within the class line ranges. Do not modify lines outside those ranges.
- For OPTION B: the architect's inferences are guides only. The test-run filter is authoritative.
- The multi-line `[Fact(\n  Skip = "...")]` format in CopyEngineB75Tests must not be altered. The 7 multi-line entries at lines 4538, 4546, 4554, 4562, 4844, 4852, 4861 span two source lines each.
- BwaveCycTaR7HelperTests has a field initializer `private readonly CopyEngine _engine = CopyEngine.Instance;` at line 5274. This means the class-level instantiation triggers TypeInit for all tests. All 34 active [Fact] in this class fail TypeInit.

---

---

# T2 -- OPTION B Individual Skip (Ticket 2 of 2)

**Ticket ID:** T2  
**Title:** Apply NT8-runtime Skip to individually-identified TypeInit tests in 3 mixed classes (OPTION B only)

---

## Spec Requirement IDs Satisfied

- PLAN-SEC5-OPTION-B: B79CancelRaceGuardTests (5 TypeInit of 64), BwaveCycT1R1BeHelperTests (2 TypeInit of 25), BwaveCycTaR6HelperTests (6 TypeInit of 17)
- PLAN-SEC6: OPTION B pre-analysis for all three classes
- PLAN-SEC7: Ticket 2 scope definition
- PLAN-SEC8: Baseline and SCAN-5 Ticket 2 target
- PLAN-SEC9: All constraints
- PLAN-SEC10: 7-scan checklist
- PLAN-SEC13: Hard-link sync after edit

---

## Scope: Test Classes in This Ticket

| Class | Strategy | Line Range | Active [Fact] | TypeInit Target | Lane A Tests (DO NOT TOUCH) |
|---|---|---|---|---|---|
| BwaveCycTaR6HelperTests | OPTION B | 7110-7268 | 17 | 6 | 11 |
| B79CancelRaceGuardTests | OPTION B | 5823-6455 | 64 | 5 | 59 |
| BwaveCycT1R1BeHelperTests | OPTION B | 6469-6678 | 25 | 2 | 23 |

**Classes NOT in scope for this ticket (do not touch):**  
All classes not listed above. In particular:
- All OPTION A classes modified in T1 (do not re-touch)
- B79BeAllTargetSnapshotTests and B79BeReplaceAttemptGuardTests (modified in T1 -- do not re-touch)
- BwaveCycTaR2HelperTests (lines 6686-6811) -- Lane A, DO NOT TOUCH
- BwaveCycTaR3HelperTests (lines 6812-7109) -- Lane A, DO NOT TOUCH

---

## Pre-Conditions

Engineer MUST measure the post-T1 baseline at the start of this ticket:

```powershell
dotnet test src/PropTraderTools/ --no-build 2>&1 | Select-String "Total|Passed|Failed|Skipped"
```

Record the output. Expected (T1 complete): `Total: 501 | Passed: >= 19 | Failed: <= 157 | Skipped: >= 325`

If T1 SCAN-5 was not met (skipped < 325 or failed > 157), STOP and complete T1 first.

---

## Method-Level Changes

### OPTION B: BwaveCycTaR6HelperTests (lines 7110-7268)

**MANDATORY first step:** Run the test filter and record which 6 tests fail with TypeInitializationException:

```powershell
dotnet test src/PropTraderTools/ --no-build --filter "FullyQualifiedName~BwaveCycTaR6HelperTests" 2>&1
```

**Expected outcome:** 6 tests fail TypeInit, 11 tests pass.

Known TypeInit-trigger candidates identified by architect (calls `m.Invoke(null, ...)` executing actual method code):

| Method Name | Line | Trigger Pattern |
|---|---|---|
| `ExtractLegSuffix_ShouldReturnNull_WhenLeaderNameHasNoTrailingDigit` | 7146 | `m.Invoke(null, new object[] { "Stop" })` |
| `ExtractLegSuffix_ShouldReturnDigit_WhenLeaderNameEndsWithDigit` | 7156 | `m.Invoke(null, new object[] { "Stop1" })` |
| `IsPositionStateRelevant_ShouldReturnFalse_WhenStateIsWorking` | 7226 | `m.Invoke(null, new object[] { OrderState.Working })` |
| `IsPositionStateRelevant_ShouldReturnTrue_WhenStateIsFilled` | 7235 | `m.Invoke(null, new object[] { OrderState.Filled })` |
| `IsPositionStateRelevant_ShouldReturnTrue_WhenStateIsPartFilled` | 7244 | `m.Invoke(null, new object[] { OrderState.PartFilled })` |

The 6th TypeInit test must be identified by the filter run. The remaining tests only call `GetStaticMethod`/`GetInstanceMethod` + `Assert.NotNull` or check parameter counts and do not invoke method bodies.

**Apply Skip ONLY to the 6 tests confirmed TypeInit by the filter run.**  
Do NOT apply Skip to the 11 passing Lane A tests.

**Exact Skip line:**
```csharp
[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]
```

**Verification step:** After applying, re-run the filter. Confirm:
- 6 tests show as `Skipped`
- 11 tests still show as `Passed`

---

### OPTION B: B79CancelRaceGuardTests (lines 5823-6455)

**MANDATORY first step:** Run the test filter and record which 5 tests fail with TypeInitializationException:

```powershell
dotnet test src/PropTraderTools/ --no-build --filter "FullyQualifiedName~B79CancelRaceGuardTests" 2>&1
```

**Expected outcome:** 5 tests fail TypeInit, 59 tests pass (Lane A tests).

Known TypeInit-trigger candidates identified by architect:

| Method Name | Line | Trigger Pattern |
|---|---|---|
| `T_DW_B79_09_01_CancelQxBrackets2Param_HasRemoveAllGuard` | 5850 | `method.GetMethodBody().GetILAsByteArray()` on CopyEngine method |
| `T_DW_B79_09_02_CancelQxBrackets3Param_HasRemoveAllGuard` | 5882 | `method.GetMethodBody().GetILAsByteArray()` on CopyEngine method |
| `T_DW_B79_09_03_CancelStaleBracketsLocal_HasRemoveAllGuard` | 5919 | `method.GetMethodBody().GetILAsByteArray()` on PttBreakEven method |
| `B132_LaneB_DiagnosticMode_FieldExists` | 5946 | `field.GetValue(null)` on static CopyEngine field |

The 5th TypeInit test must be identified by the filter run. The other 59 tests use only `GetMethod(...)` / `Assert.NotNull(m)` patterns and do not trigger cctor.

**Apply Skip ONLY to the 5 tests confirmed TypeInit by the filter run.**  
DO NOT TOUCH the 59 Lane A tests in this class.

**Exact Skip line:**
```csharp
[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]
```

**Verification step:** After applying, re-run the filter. Confirm:
- 5 tests show as `Skipped`
- 59 tests still show as `Passed`

---

### OPTION B: BwaveCycT1R1BeHelperTests (lines 6469-6678)

**MANDATORY first step:** Run the test filter and record which 2 tests fail with TypeInitializationException:

```powershell
dotnet test src/PropTraderTools/ --no-build --filter "FullyQualifiedName~BwaveCycT1R1BeHelperTests" 2>&1
```

**Expected outcome:** 2 tests fail TypeInit, 23 tests pass.

Known TypeInit-trigger candidates identified by architect (all call `CopyEngine.Instance` directly):

| Method Name | Line | CopyEngine.Instance at Line |
|---|---|---|
| `SelectBeRefPriceByDirection_ShouldReturnBid_WhenLongAndBidIsPositive` | 6500 | 6504 |
| `SelectBeRefPriceByDirection_ShouldReturnAsk_WhenLongAndBidIsZero` | 6510 | 6514 |
| `SelectBeRefPriceByDirection_ShouldReturnAsk_WhenShortAndAskIsPositive` | 6520 | 6524 |
| `SelectBeRefPriceByDirection_ShouldReturnBid_WhenShortAndAskIsZero` | 6530 | 6534 |
| `GetSenderAccountName_ShouldReturnEmpty_WhenSenderIsNull` | 6565 | 6569 |
| `GetSenderAccountName_ShouldReturnEmpty_WhenSenderIsNotAccount` | 6575 | 6579 |

Mission brief states 2 TypeInit failures. Only 2 of these 6 candidates are currently failing
(the other 4 may be Lane A failures or may pass). The filter run is the only authoritative source.

**Apply Skip ONLY to the 2 tests confirmed TypeInit by the filter run.**  
DO NOT TOUCH the other 23 tests in this class.

**Exact Skip line:**
```csharp
[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]
```

**Verification step:** After applying, re-run the filter. Confirm:
- 2 tests show as `Skipped`
- 23 tests still show as `Passed`

---

## Post-Conditions (after Ticket 2 complete -- full epic done)

Run the full test suite:

```powershell
dotnet test src/PropTraderTools/ --no-build 2>&1 | Select-String "Total|Passed|Failed|Skipped"
```

**Expected SCAN-5 targets (full epic):**

| Metric | Required |
|---|---|
| Total | 501 (unchanged -- no tests added or removed) |
| Passed | >= 19 (must NOT decrease from baseline) |
| Failed | <= 144 (T1 target 157 - 13 new T2 Skips) |
| Skipped | >= 338 (T1 target 325 + 13 new T2 Skips) |

Delta from baseline: +306 Skipped, -306 Failed.

---

## 7-Scan Checklist -- Ticket 2

Engineer must run and PASS all 7 scans before marking T2 complete.

```
SCAN-1: grep -c "lock(" src/PropTraderTools/CopyEngineTests.cs
        -> Required: 0
        -> Failure action: revert and report

SCAN-2: Select-String -Pattern "[^\x00-\x7F]" src/PropTraderTools/CopyEngineTests.cs
        (or equivalent non-ASCII check on all changed lines)
        -> Required: 0 non-ASCII characters in any [Fact(Skip = "...")] line added in this ticket
        -> Failure action: find and remove non-ASCII character, re-run

SCAN-3: dotnet build src/PropTraderTools/ --no-restore 2>&1 | Select-String "Error"
        -> Required: 0 CS error lines
        -> Failure action: fix compilation error before proceeding

SCAN-4: dotnet build src/PropTraderTools/ 2>&1 | Select-String " Error\(s\)"
        -> Required: "0 Error(s)" in build summary
        -> Failure action: fix compilation error before proceeding

SCAN-5: dotnet test src/PropTraderTools/ --no-build 2>&1 | Select-String "Total|Passed|Failed|Skipped"
        -> Required: Total=501, Passed>=19, Failed<=144, Skipped>=338
        -> Failure action: if Passed dropped -> revert; if Skipped count wrong -> recount applied Skips

SCAN-6: powershell -File .\deploy-sync.ps1
        -> Required: output contains "SYNC COMPLETE"
        -> Failure action: check deploy-sync.ps1 for errors, do NOT proceed without SYNC COMPLETE

SCAN-7: $f = Get-Item "src\PropTraderTools\CopyEngineTests.cs"; $f.LinkType + " count=" + $f.HardLinkCount
        -> Required: LinkType = "HardLink" AND HardLinkCount >= 2
        -> IMPORTANT: count=1 means NOT hard-linked (regular file -- SCAN-6 sync FAILED)
        -> Failure action: re-run deploy-sync.ps1, verify SYNC COMPLETE, then re-check SCAN-7
```

---

## Constraints Checklist -- Ticket 2

- [ ] ONLY `src/PropTraderTools/CopyEngineTests.cs` modified. No other file touched.
- [ ] Zero production code changes.
- [ ] Skip string is exactly: `[Fact(Skip = "NT8-runtime: CopyEngine.cctor requires NT8 host")]`
- [ ] Skip string is ASCII-only (no Unicode, no curly quotes, no em-dashes).
- [ ] No `lock()` anywhere in the file.
- [ ] No `throw` statements added.
- [ ] No new `SkipReason` constants, variables, or fields added.
- [ ] No existing `[Fact(Skip...)]` annotations modified (T1 changes must be preserved).
- [ ] OPTION B: filter run BEFORE applying any Skip for each class.
- [ ] OPTION B: Skip applied ONLY to TypeInit-failing tests (not passing tests).
- [ ] Lane A tests in B79CancelRaceGuardTests (59 tests) are NOT touched.
- [ ] Non-TypeInit tests in BwaveCycT1R1BeHelperTests (23 tests) are NOT touched.
- [ ] Non-TypeInit tests in BwaveCycTaR6HelperTests (11 tests) are NOT touched.

---

## Engineer Notes -- Ticket 2

- Record actual pre-change baseline from `dotnet test` run at the start of this ticket (must reflect T1 completed state).
- For all three classes: the test-run filter is the authoritative source of which tests fail TypeInit. The architect's candidate lists are guides derived from static analysis only.
- When applying Skip in B79CancelRaceGuardTests, the class spans lines 5823-6455 with 64 tests. Only 5 of those 64 are TypeInit failures. The other 59 are Lane A failures (a separate epic) -- do NOT touch them.
- BwaveCycTaR2HelperTests (lines 6686-6811) and BwaveCycTaR3HelperTests (lines 6812-7109) are explicitly out of scope. Do not touch any test in those classes.

---

---

# Summary Table

| Ticket | Classes | Strategy | TypeInit Skips | SCAN-5 Skipped Target | SCAN-5 Failed Target |
|---|---|---|---|---|---|
| T1 | CopyEngineTests, CopyEngineB75Tests, B77QxRaceGuardTests, B78TargetDispatchTests, BwaveCycTaR7HelperTests, B79BeAllTargetSnapshotTests, B79BeReplaceAttemptGuardTests | A+B | 293 | >= 325 | <= 157 |
| T2 | BwaveCycTaR6HelperTests, B79CancelRaceGuardTests, BwaveCycT1R1BeHelperTests | B only | 13 | >= 338 | <= 144 |
| **Epic total** | **10 classes** | | **306** | **>= 338** | **<= 144** |

---

TICKETS_COMPLETE
