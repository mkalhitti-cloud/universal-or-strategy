Design a lock-free Single Producer Single Consumer data structure in C# aimed at sub-5ns execution. Focus on L1-Data cache line isolation (64-byte boundaries) for head and tail indices to prevent false sharing. Propose a method to swap unmanaged pointers for pre-allocated struct instances to eliminate garbage collection pressure and allocation overhead. Ensure memory consistency using volatile fields and appropriate barriers for a x64 architecture. Evaluate the use of integrated generation counters per slot for fine-grained state tracking without global synchronization. Provide a technical summary of the implementation and expected latency improvements. Required output format: Agent Name, Conclusion on generation counters, Design Name, Technical Logic summary, Estimated Latency in nanoseconds, and Efficiency percentage.
