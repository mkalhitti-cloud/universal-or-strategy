<Window x:Class="V9_ExternalControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:V9_ExternalControl"
        mc:Ignorable="d"
        Title="V9 Universal OR Strategy - External Control" Height="600" Width="800" Background="#1E1E1E">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" /> <!-- Panel 1 & 2 -->
            <RowDefinition Height="Auto" /> <!-- Panel 3 -->
            <RowDefinition Height="Auto" /> <!-- Status Bar -->
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Panel 1: TOS RTD Status -->
            <GroupBox Header="TOS RTD Status" Margin="5" Foreground="White" BorderBrush="#444">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Ellipse x:Name="TosStatusLed" Width="20" Height="20" Fill="Gray" Stroke="Black" StrokeThickness="1" Margin="0,0,10,0"/>
                        <TextBlock x:Name="TosConnectionText" Text="Disconnected" Foreground="White" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                    </StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Symbol:" Foreground="#AAA" Grid.Row="0" Grid.Column="0" Margin="0,2"/>
                        <TextBlock x:Name="TxtSymbol" Text="--" Foreground="White" FontWeight="Bold" Grid.Row="0" Grid.Column="1" Margin="10,2"/>

                        <TextBlock Text="Last Price:" Foreground="#AAA" Grid.Row="1" Grid.Column="0" Margin="0,2"/>
                        <TextBlock x:Name="TxtLastPrice" Text="0.00" Foreground="#00FF00" FontWeight="Bold" Grid.Row="1" Grid.Column="1" Margin="10,2"/>

                        <TextBlock Text="EMA 9:" Foreground="#AAA" Grid.Row="2" Grid.Column="0" Margin="0,2"/>
                        <TextBlock x:Name="TxtEma9" Text="0.00" Foreground="Cyan" Grid.Row="2" Grid.Column="1" Margin="10,2"/>

                        <TextBlock Text="EMA 15:" Foreground="#AAA" Grid.Row="3" Grid.Column="0" Margin="0,2"/>
                        <TextBlock x:Name="TxtEma15" Text="0.00" Foreground="Magenta" Grid.Row="3" Grid.Column="1" Margin="10,2"/>

                        <TextBlock Text="OR High:" Foreground="#AAA" Grid.Row="4" Grid.Column="0" Margin="0,2"/>
                        <TextBlock x:Name="TxtOrHigh" Text="0.00" Foreground="Yellow" FontWeight="Bold" Grid.Row="4" Grid.Column="1" Margin="10,2"/>

                        <TextBlock Text="OR Low:" Foreground="#AAA" Grid.Row="5" Grid.Column="0" Margin="0,2"/>
                        <TextBlock x:Name="TxtOrLow" Text="0.00" Foreground="Yellow" FontWeight="Bold" Grid.Row="5" Grid.Column="1" Margin="10,2"/>
                    </Grid>
                    
                    <TextBlock x:Name="TxtTosLastUpdate" Text="Last Update: --" Foreground="#666" Margin="0,20,0,0" FontSize="10"/>
                </StackPanel>
            </GroupBox>

            <!-- Panel 2: Copy Trading Dashboard -->
            <GroupBox Header="Copy Trading Dashboard" Grid.Column="1" Margin="5" Foreground="White" BorderBrush="#444">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="10">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Status: " Foreground="#AAA"/>
                            <Ellipse x:Name="CopyStatusLed" Width="12" Height="12" Fill="Gray" Stroke="Black" StrokeThickness="1" Margin="5,0"/>
                            <TextBlock x:Name="TxtCopyStatus" Text="Inactive" Foreground="White" FontWeight="Bold" Margin="5,0"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Heartbeat: " Foreground="#AAA"/>
                            <TextBlock x:Name="TxtHeartbeat" Text="OK" Foreground="LimeGreen" FontWeight="Bold" Margin="5,0"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Connected Accounts: " Foreground="#AAA"/>
                            <TextBlock x:Name="TxtAccountsConnected" Text="0/20" Foreground="White" FontWeight="Bold" Margin="5,0"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="Aggregate P&amp;L: " Foreground="#AAA"/>
                            <TextBlock x:Name="TxtAggregatePnL" Text="$0.00" Foreground="White" FontWeight="Bold" Margin="5,0"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                             <TextBlock Text="Errors: " Foreground="#AAA"/>
                             <TextBlock x:Name="TxtErrorCount" Text="0" Foreground="Red" FontWeight="Bold" Margin="5,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Top Accounts Table -->
                    <DataGrid x:Name="GridTopAccounts" Grid.Row="1" Margin="5" AutoGenerateColumns="False" 
                              Background="#252525" RowBackground="#252525" Foreground="White" 
                              HeadersVisibility="Column" IsReadOnly="True" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Account" Binding="{Binding AccountName}" Width="*" />
                            <DataGridTextColumn Header="P&amp;L" Binding="{Binding PnL, StringFormat=C2}" Width="*" />
                            <DataGridTextColumn Header="Status" Binding="{Binding IsConnected}" Width="Auto" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Panel 3: Manual Trade Controls -->
        <GroupBox Header="Manual Controls" Grid.Row="1" Margin="5" Foreground="White" BorderBrush="#444">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <StackPanel VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="Qty:" Foreground="#AAA" HorizontalAlignment="Center"/>
                    <TextBox x:Name="InputQty" Text="1" Width="40" HorizontalContentAlignment="Center" Background="#333" Foreground="White" BorderBrush="#555"/>
                </StackPanel>
                
                <Button Content="LONG" Width="80" Height="40" Margin="5" Background="#008800" Foreground="White" FontWeight="Bold" Click="Button_Click_Long"/>
                <Button Content="SHORT" Width="80" Height="40" Margin="5" Background="#880000" Foreground="White" FontWeight="Bold" Click="Button_Click_Short"/>
                <Button Content="CLOSE ALL" Width="80" Height="40" Margin="5,5,30,5" Background="#555" Foreground="White" Click="Button_Click_Close"/>
                
                <Button Content="STOP" Width="80" Height="40" Margin="5" Background="Red" Foreground="White" FontWeight="Bold" FontSize="14" Click="Button_Click_Stop"/>
            </StackPanel>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Background="#222" Foreground="#888">
            <StatusBarItem>
                <TextBlock x:Name="TxtStatusBar" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
