name: Markdown Link Check
on: [push, pull_request]

jobs:
  markdown-link-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check Links
        uses: JustinBeckwith/link-check@v1.0.10
        with:
          configFile: '.github/mlc_config.json'
